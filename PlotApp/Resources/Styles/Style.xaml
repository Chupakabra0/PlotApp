<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:oxy="http://oxyplot.org/wpf">

    <Style TargetType="Window" x:Key="DefaultWindow">
        <Setter Property="Background" Value="{StaticResource AppBlueBrush}"></Setter>
        <Setter Property="Foreground" Value="{StaticResource AppBlackBrush}"></Setter>
    </Style>

    <Style TargetType="oxy:PlotView">
        <Setter Property="Background" Value="{StaticResource AppWhiteBrush}"></Setter>
    </Style>

    <Style TargetType="ComboBox" x:Key="DefaultComboBox">
        <Setter Property="MinWidth" Value="100"></Setter>
        <Setter Property="FontSize" Value="20"></Setter>
        <Setter Property="Margin" Value="2"></Setter>
        <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
    </Style>

    <Style TargetType="TextBox" x:Key="DefaultTextBox">
        <Setter Property="MinWidth" Value="100"></Setter>
        <Setter Property="FontSize" Value="20"></Setter>
        <Setter Property="Margin" Value="2"></Setter>
        <Setter Property="Padding" Value="5, 2.5"></Setter>
        <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
    </Style>

    <Style TargetType="TextBox" x:Key="SmallTextBox" BasedOn="{StaticResource DefaultTextBox}">
        <Setter Property="MinWidth" Value="48"></Setter>
    </Style>

    <Style TargetType="Button" x:Key="DefaultButton">

        <Setter Property="Padding" Value="15, 5"></Setter>
        <Setter Property="Margin" Value="10"></Setter>

        <Setter Property="Background" Value="{StaticResource AppGreenBrush}"></Setter>
        <Setter Property="Foreground" Value="{StaticResource AppWhiteBrush}"></Setter>

        <Setter Property="Height" Value="46"></Setter>
        <!--<Setter Property="Width" Value="356"></Setter>-->

        <!--<Setter Property="FontFamily" Value="{StaticResource PoppinsBold}"></Setter>-->
        <Setter Property="FontWeight" Value="Bold"></Setter>
        <Setter Property="FontSize" Value="14"></Setter>

        <Setter Property="BorderThickness" Value="1"></Setter>
        <Setter Property="BorderBrush" Value="{StaticResource AppDarkGreenBrush}"></Setter>

        <Setter Property="Template">

            <Setter.Value>

                <ControlTemplate TargetType="Button">

                    <Grid>

                        <Border x:Name="ButtonBorder"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">

                            <TextBlock HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       Margin="{TemplateBinding Padding}"
                                       Focusable="False" 
                                       Text="{TemplateBinding Content}"
                                       FontFamily="{TemplateBinding FontFamily}">
                            </TextBlock>

                        </Border>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppDarkGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Name="Disable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Name="Enable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>

        </Setter>

    </Style>

    <Style TargetType="Button" x:Key="EditButton" BasedOn="{StaticResource DefaultButton}">
        <Setter Property="Background" Value="{StaticResource AppWhiteBrush}"></Setter>
        <Setter Property="Foreground" Value="{StaticResource AppBlackBrush}"></Setter>
        <Setter Property="Padding" Value="7, 3"></Setter>
        <Setter Property="Margin" Value="5"></Setter>
        
        <Setter Property="BorderThickness" Value="0.5"></Setter>
        <Setter Property="BorderBrush" Value="{StaticResource AppBlackBrush}"></Setter>

        <Setter Property="Template">

            <Setter.Value>

                <ControlTemplate TargetType="Button">

                    <Grid>

                        <Border x:Name="ButtonBorder"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">

                            <TextBlock HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       Margin="{TemplateBinding Padding}"
                                       Focusable="False" 
                                       Text="{TemplateBinding Content}"
                                       FontFamily="{TemplateBinding FontFamily}">
                            </TextBlock>

                        </Border>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppWhiteColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppWhiteColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppDarkGreenColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppWhiteColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Name="Disable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Name="Enable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppWhiteColor}" Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="Background.Color">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>

        </Setter>
    </Style>

    <Style TargetType="Button" x:Key="TrashButton" BasedOn="{StaticResource EditButton}">
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>

                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label HorizontalAlignment="Center" VerticalAlignment="Center"
                               HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                            <Label.Content>
                                <Path x:Name="ButtonPath"
                                      Fill="Black" Stroke="White"
                                      Data="M 20.398438 2.398438 L 16.199219 2.398438 L 15 1.199219 L 9 1.199219 L 7.800781 2.398438 L 3.601562 2.398438 L 3.601562 4.800781 L 20.398438 4.800781 Z M 4.800781 20.398438 C 4.800781 21.726562 5.875 22.800781 7.199219 22.800781 L 16.800781 22.800781 C 18.125 22.800781 19.199219 21.726562 19.199219 20.398438 L 19.199219 6 L 4.800781 6 Z M 4.800781 20.398438">
                                </Path>
                            </Label.Content>
                        </Label>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppBlackColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppDarkGrayColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppDarkGrayColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppBlackColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="True">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="True">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppGrayColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <RemoveStoryboard BeginStoryboardName="Enable"></RemoveStoryboard>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppBlackColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                        <MultiDataTrigger>

                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                                           Value="False">
                                </Condition>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}"
                                           Value="False">
                                </Condition>
                            </MultiDataTrigger.Conditions>

                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Name="Disable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppDarkGrayColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>

                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Name="Enable">
                                    <Storyboard>
                                        <ColorAnimation To="{StaticResource AppBlackColor}"
                                                        Duration="0:0:0.25"
                                                        Storyboard.TargetName="ButtonPath"
                                                        Storyboard.TargetProperty="(Path.Fill).(SolidColorBrush.Color)">
                                        </ColorAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>

                        </MultiDataTrigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
    </Style>

    <Style TargetType="DataGrid" x:Key="DefaultDataGrid">
        <Setter Property="AutoGenerateColumns" Value="False"></Setter>
        <Setter Property="CanUserAddRows" Value="True"></Setter>
        <Setter Property="CanUserDeleteRows" Value="True"></Setter>
        <Setter Property="SelectionUnit" Value="FullRow"></Setter>
        <Setter Property="SelectionMode" Value="Extended"></Setter>

        <Setter Property="CanUserReorderColumns" Value="False"></Setter>
        <Setter Property="CanUserSortColumns" Value="True"></Setter>
        <Setter Property="CanUserResizeColumns" Value="False"></Setter>
        <Setter Property="CanUserResizeRows" Value="False"></Setter>
        <Setter Property="IsReadOnly" Value="False"></Setter>

        <Setter Property="Margin" Value="10"></Setter>
        <Setter Property="FontSize" Value="16"></Setter>

        <!--<Setter Property="HorizontalAlignment" Value="Center"></Setter>
        <Setter Property="VerticalAlignment" Value="Center"></Setter>-->
    </Style>

</ResourceDictionary>